#include <stdio.h>
int main()
{
    int a,b;
    printf("enter the two numbers");
    scanf("%d",&a,&b);
    if (a==b)
    {
        printf("the two numbers are equal \n");
    }
    else {
        printf("the two numbers are not equal");
    }
    return 0;
}
